# Byte-exact files: recorded hashes cover raw bytes (chain_hash over the
# chain file, sha256 over artifacts, sync-check over the vendored engine).
# EOL conversion on checkout would silently break them on Windows.
fixtures/** -text
verifier/forgeproof.py -text
